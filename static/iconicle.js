// Iconicle Game Class

class IconicleMeme {
    constructor(image_data){
        this.width = image.width;
        this.height = image.height;
        this.image = getImageData(image_data);
        this.row = 4;
        this.column = 4;
        this.tiles = 16;
        this.rowHeight = this.width / this.row;
        this.colHeight = this.height / this.row;
    }
    getImageData(image_data){

    }

    createBoardTiles(rowHeight, colHeight){
        
    }
}